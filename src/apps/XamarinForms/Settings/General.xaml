<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Chroomsoft.Top2000.Apps.Settings.General"
             xmlns:res="clr-namespace:Chroomsoft.Top2000.Apps.Globalisation"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             Title="{Binding [Settings], Source={x:Static res:Translator.Instance}}">
    <ContentPage.Content>
        <StackLayout Margin="20,5,0,0">
            <Label Text="{Binding [ThemeMode], Source={x:Static res:Translator.Instance}}"
                   Style="{StaticResource SmallLabelStyle}" />
            <Picker x:Name="ThemePicker" Title="{Binding [SelectTheme], Source={x:Static res:Translator.Instance}}"
                    SelectedIndexChanged="OnThemeChanged"
                    TextColor="{DynamicResource PrimaryTextColor}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
